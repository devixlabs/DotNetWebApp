#!/usr/bin/env bash
export SHELLCHECK_SHELL=bash

export APP_DATABASE_PASSWORD="${APP_DATABASE_PASSWORD}"

# Override defaults with `.env.local` when present 
if [[ -f .env.local ]]; then 
  set -a 
  # shellcheck source=/dev/null 
  source .env.local 
  set +a 
fi
