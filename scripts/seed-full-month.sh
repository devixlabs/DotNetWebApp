#!/usr/bin/env bash
# Placeholder script for seed data generation
# Seeds data for the full month as configured in sql/seed.sql
# This script is referenced by the Makefile check target.

set -e

# No-op: seed data is now committed directly to sql/seed.sql
# Run 'make seed' to apply it via the database.

echo "✓ Seed data script validated"
